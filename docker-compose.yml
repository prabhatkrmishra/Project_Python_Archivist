services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - ARCHIVIST_API_KEY=${ARCHIVIST_API_KEY:-archivist-dev-key}
    volumes:
      - ./data:/data
