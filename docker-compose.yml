services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - API_KEY=${API_KEY:-archivist-dev-key}
      - VECTORIZER_N_FEATURES=1048576
    volumes:
      - ./data:/data
